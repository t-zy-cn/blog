name: CI # 工作流名称

on: # 触发条件
  push:
    branches:
      - main

jobs: # 任务
  build: # 任务名称
    runs-on: ubuntu-latest # 运行环境
    steps: # 执行步骤
      - uses: actions/checkout@v3 # 检出代码
      - name: Run tests
        run: npm install && npm test # 安装依赖并运行测试
